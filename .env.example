# Vercel has used both variable name pairs for Upstash Redis integrations.
# linkvault checks KV_REST_API_* first, then falls back to UPSTASH_REDIS_REST_*.
KV_REST_API_URL=
KV_REST_API_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

APP_PASSWORD=
